# Database connection (PostgreSQL). Use Docker: npm run db:up
DATABASE_URL=postgresql://admin:password@localhost:5432/bowers

# Session encryption secret (generate a random 64+ character string)
SESSION_SECRET=change-me-to-a-random-secret-at-least-64-characters-long

# Server port (defaults to 3000)
PORT=3000

# Environment (development | production)
NODE_ENV=development

# CORS allowed origins (comma-separated, only enforced in production)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Pinata JWT for IPFS uploads (get from https://app.pinata.cloud)
# PINATA_JWT=your-pinata-jwt
